# This file contains metadata for your plugin.

[general]
name=NoWires
qgisMinimumVersion=4.0
qgisMaximumVersion=4.99
description=Radio propagation analysis and terrain tools with P2P, Batch P2P, Coverage Analysis, Coverage Comparison, and Contour Lines
version=1.5.5
author=Bortre Tenamo
email=tedaks@gmail.com

about=NoWires is a QGIS plugin for radio propagation analysis powered by NTIA's Irregular Terrain Model (ITM), using the Qt 6 / PyQt 6 runtime bundled with QGIS 4. It provides point-to-point path loss analysis with Fresnel zone visualization, batch P2P analysis (one-to-many and many-to-one), unified Coverage Analysis heatmap prediction with raster-derived range metrics, Coverage Comparison producing delta rasters, and contour line generation. Clutter correction supports simple per-category losses and advanced mode (saalos vegetation model, ITU-R P.2108 for built/rural). DEM data is automatically downloaded from the Copernicus GLO-30 dataset hosted on AWS Open Data. The plugin bundles the ITM library from https://github.com/tedaks/pyitm and adapts code from https://github.com/tedaks/nowires and https://github.com/tedaks/ContourLines. See NOTICE.md for attribution and third-party licensing details.

hasProcessingProvider=yes

changelog=
    Version 1.5.5
    - Fix coverage multiprocessing silently falling back to sequential on macOS. Three distinct bugs all surfacing as 'Multiprocessing unavailable'. (1) Shared-memory name too long for macOS PSHMNAMLEN=31: truncated UUID hex suffix from 20 to 16 chars. (2) Cross-process cancel signal removed entirely; multiprocessing.Event() failed under spawn, Manager().Event() failed with EOFError on macOS QGIS. Cancel now relies on main-thread break between batches (~320ms worst case). (3) QGIS-bundled python3.12 has sys.prefix baked to CI builder path; spawned workers couldn't find stdlib ('No module named encodings'). configure_macos_multiprocessing now sets PYTHONHOME=sys.prefix so workers inherit a working stdlib pointer. Added _can_spawn() validation and NOWIRES_PYTHON_EXE env override.
    - Measured 4-6× speedup on the in-container benchmark (~7k px/s → 31-48k px/s); macOS users will see equivalent gains.
    Version 1.5.4
    - Fix macOS SIGABRT crash when running Coverage Analysis with ALLOW_THREADING enabled: defer show_coverage_legend() from processAlgorithm (worker thread) to postProcessAlgorithm (main thread). Cocoa rejects QWidget creation off the main thread.
    - Extract _validate_dem_coverage to coverage_dem_validate.py
    Version 1.5.3
    - Add ALLOW_THREADING opt-in on base algorithm; Coverage, Batch P2P, and Coverage Comparison now run off the main thread so the QGIS UI stays responsive during heavy computation
    - Add per-tile wall-clock budget (180s default) in tile_download_base to cap slow trickles where socket_timeout alone never fires
    - Add cache size confirmation dialog before clearing DEM/WorldCover tiles, with new get_cache_size() and format_cache_size() helpers
    - Add ClutterParamBundle plus extract_clutter_params() and LinkBudgetBundle plus extract_link_budget_params() in shared_params; deduplicates parameter extraction across coverage, batch, and P2P algorithms
    - Add PDF report output for Coverage Analysis (OUTPUT_REPORT_PDF), rendered via Qt QTextDocument + QPrinter
    - Add Preview Antenna Pattern plugin menu action; new AntennaPatternPreviewDialog renders a polar plot of any pattern CSV
    - Add build_initial_clutter_context() factory; deduplicates ClutterLossContext construction between algorithm_coverage and coverage_engine
    - Add NOWIRES_WINDOWS_MP env var to opt Windows into multiprocessing (off by default)
    - Add golden-file regression tests for CSV/JSON/HTML report export and a PDF writer test
    - Change contour_smoothing to use defusedxml _safe_parse via an explicit wrapper; removes a global ET.parse monkey-patch
    - Change elevation sampling fallback in coverage_engine to vectorize row-by-row via sample_line (192x fewer Python calls at default grid size for mocked grids)
    - Change tile_download_base HTTP failures to surface status code and retry timing through feedback.pushInfo/pushWarning
    - Change _init_cov_pool to reset stale state instead of raising RuntimeError when a worker is reused across runs
    - Change nowires.py menu registration to a single action_specs table; net 50 fewer lines
    - Improve sequential coverage progress reporting from 100 buckets to 200 and multiprocessing progress from every 50 chunks to every 5
    - Annotate all 9 previously-unexplained # type: ignore directives with brief reasons
    Version 1.5.2
    - Add clear_pattern_cache() API for reloading antenna pattern CSV files
    - Add GDAL UseExceptions() at plugin startup for consistent error handling
    - Add NaN-aware elevation interpolation in batch P2P using nan_utils
    - Add contour CRS fallback when context project is unavailable
    - Add batch algorithm multipart geometry handling with debug logging
    - Add 8 new integration and hypothesis property-based test suites
    - Add hardened CI pipeline: pip-audit, concurrency groups, version/changelog enforcement
    - Add coverage collection in integration tests with two-step pytest isolation
    - Fix METERS_PER_FOOT constant incorrectly used as feet-to-meter multiplier in contour generation
    - Fix mypy compliance with type annotations across 20+ modules
    - Fix CI: track missing mypy.ini, remove continue-on-error, drop QGIS 3.34 from integration matrix
    - Fix missing trailing newlines, import ordering, and dead code across modules
    - Updated CONTRIBUTING.md and AGENTS.md with CI pipeline documentation
    Version 1.5.1
    - Add advanced clutter correction mode with saalos vegetation and ITU-R P.2108 site-general clutter loss
    - Add canopy/clutter height override for advanced mode
    - Add tx_cch_m and rx_cch_m to P2P report payload
    - Add per-pixel TX clutter computation and caching in coverage pipeline for advanced mode
    - Add per-task TX clutter override support in batch and comparison workflows
    - Add Very Strong (-30 dBm) color stop to coverage palette for near-transmitter pixels
    - Fix ModuleNotFoundError for clutter_constants at QGIS runtime (absolute vs relative import)
    - Fix coverage heatmap missing color near transmitter (palette had no stop above -60 dBm; added Very Strong stop and +100 dBm ceiling entry)
    - Fix P2P owned clutter grid not closed after sampling (GDAL handle leak)
    - Fix TOCTOU race conditions in temp directory creation
    - Fix macOS multiprocessing compatibility
    - Fix P.2108 frequency factor: all categories now use diffraction-based scaling where clutter loss increases with frequency, consistent with P.2108-1
    - Fix coverage pool fallback to sequential mode on worker errors
    - Fix duplicate compute_terminal_clutter_losses call in coverage pipeline
    Version 1.4.0
    - Add Batch P2P Analysis algorithm (one-to-many and many-to-one)
    - Add Coverage Comparison algorithm (dual-panel delta raster)
    - Add interactive P2P profile chart with hover callouts and export
    - Add P2P report and marker outputs
    - Add coverage report outputs
    - Add reliability outputs (fade-margin classes, availability guidance)
    - Add live coverage opacity slider dialog
    - Add 3D scene tracking and opening for coverage and contour outputs
    - Add advanced clutter correction mode
    - Architecture refactor: split monolithic algorithm files into focused helper modules
    - Fix Windows access violation crash from unsafe project layer mutation in processAlgorithm
    - Fix "layer not correctly generated" error from double-loading conflict with styled output layers
    - Fix DEM raster layers loading on top of coverage and contour outputs
    - Fix missing ANTENNA_AZ class constant that caused AttributeError
    - Fix P2P Fresnel zone, line, and profile layers rule-based symbology
    - Version 1.1.0
    - Replace separate area coverage and radius sweep tools with Coverage Analysis
    - Add raster-derived coverage range statistics
    - Improve coverage raster styling with a heatmap presentation

tags=radio, propagation, ITM, coverage, path loss, terrain, DEM, contour, GLO-30, elevation, antenna, Fresnel, batch, comparison, clutter
category=Analysis
icon=logo.png
experimental=False
deprecated=False
